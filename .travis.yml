matrix:
  include:
  - language: python
    env: NAME=pylint

install:
- |-
  [[ "$NAME" == "pylint" ]] && pip install pylint

script:
- |-
  [[ "$NAME" == "pylint" ]] \
  && pylint --indent-string="  " \
      marketplace/deployer_util/*
